import { LocaleType } from "@/types/global";

export const LOCALE_TYPE = Object.freeze(<Record<string, LocaleType>>{
  EN: "en",
  UA: "ua",
});

export const LOCALE_NAME = Object.freeze(<Record<string, string>>{
  EN: "english",
  UA: "ukrainian",
});
